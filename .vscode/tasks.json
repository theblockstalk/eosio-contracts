{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Shapes polymorphic class",
            "type": "shell",
            "group": "build",
            "command": "eosio-cpp -I contract/include -R resource -o build/shapes.wasm -contract shapes ./contract/src/shapes.cpp --abigen",
            "options": {
                "cwd": "./shapes_polymorphic_type"
            },
        },
    ]
}